# FakeScope Test Dependencies
# Minimal requirements for running unit tests in CI/CD
# Last updated: 2025-12-14

# ============================================
# Core Data Science Libraries (Lightweight)
# ============================================
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.1.0
scikit-learn>=1.3.0,<2.0.0

# ============================================
# Testing Tools
# ============================================
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# ============================================
# Code Quality Tools
# ============================================
black>=25.0.0
isort>=5.0.0
flake8>=6.0.0

# ============================================
# Utilities
# ============================================
python-dotenv>=1.0.0,<2.0.0
requests>=2.32.3,<3.0.0
joblib>=1.4.2,<2.0.0
jsonschema>=4.23.0,<5.0.0

# ============================================
# Lightweight NLP (for basic tests)
# ============================================
nltk>=3.8.1,<4.0.0

# ============================================
# Statistical Analysis
# ============================================
scipy>=1.10.0,<2.0.0

# ============================================
# Data Parsing
# ============================================
beautifulsoup4>=4.12.3,<5.0.0

# ============================================
# Web Framework (needed for API tests)
# ============================================
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# ============================================
# LLM APIs (lightweight, needed for tests)
# ============================================
openai>=1.52.2,<2.0.0
google-generativeai>=0.8.0,<1.0.0

# ============================================
# NOTE: Heavy dependencies excluded for CI/CD
# ============================================
# The following are NOT included to save disk space:
# - torch (2GB+) - tests will be skipped in CI
# - transformers (500MB+) - tests will be skipped in CI
# - mlflow (200MB+) - not needed for unit tests
# - streamlit, gradio (UI frameworks) - not needed for unit tests
# - xgboost, shap (advanced ML) - tests will be skipped in CI
# - spacy (large NLP models) - not needed for basic tests
#
# These should be installed separately for:
# - Local development (use requirements.txt)
# - Production deployment (use requirements.txt)
# - Full test suite (use requirements.txt)
